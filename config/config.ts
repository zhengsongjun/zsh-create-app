import { AppLayoutConfig } from '../types/config';

const layoutConfig: AppLayoutConfig = {
  title: 'zsj脚手架',
  logo: '/vite.svg',
  layout: 'mix',
  contentWidth: 'Fluid',
  fixedHeader: true,
  fixSiderbar: true,
  navTheme: 'dark',
  colorPrimary: '#722ed1',
  borderRadius: 6,
};

export default layoutConfig;
